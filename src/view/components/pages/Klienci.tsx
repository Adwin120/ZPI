import CommonLayout from "../layout/CommonLayout";

interface Props {}
const Klienci: React.FC<Props> = () => {
    return <CommonLayout pageTitle="MOXLY">klienci</CommonLayout>;
};

export default Klienci;
